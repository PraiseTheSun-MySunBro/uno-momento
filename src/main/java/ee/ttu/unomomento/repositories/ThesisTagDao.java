package ee.ttu.unomomento.repositories;

import ee.ttu.unomomento.models.ThesisTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ThesisTagDao extends JpaRepository<ThesisTag, Long> {
}
